~~~{"variant":"standard","title":"Valid Docker Compose GitHub Actions Workflow","id":"89347","content":"name: Build and Push Docker Compose Images\n\non:\n  push:\n    branches: [ main ]\n  workflow_dispatch:\n\njobs:\n  build-and-push:\n    runs-on: ubuntu-latest\n\n    steps:\n      - name: Checkout repository\n        uses: actions/checkout@v3\n\n      - name: Log in to Docker Hub\n        uses: docker/login-action@v3\n        with:\n          username: ${{ secrets.DOCKERHUB_USERNAME }}\n          password: ${{ secrets.DOCKERHUB_TOKEN }}\n\n      - name: Build Docker Compose services\n        run: docker compose build\n\n      - name: Tag Backend Image\n        run: |\n          docker tag journey-app-cicd-backend:latest ${{ secrets.DOCKERHUB_USERNAME }}/journey-app-cicd-backend:latest\n\n      - name: Tag Frontend Image\n        run: |\n          docker tag journey-app-cicd-frontend:latest ${{ secrets.DOCKERHUB_USERNAME }}/journey-app-cicd-frontend:latest\n\n      - name: Push Backend Image\n        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/journey-app-cicd-backend:latest\n\n      - name: Push Frontend Image\n        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/journey-app-cicd-frontend:latest"}~~~
